REVIEW_PASSED

# Story 1.3 Code Review — Add `--dry-run-prompts` flag

Reviewed the working-tree diff of `scripts/ralph-loop.sh` and
`system/ralph-loop-system.sh` against the story's acceptance criteria, the
System Track stack rules (`system/CLAUDE.md`), and the checkpoint gate.

## Acceptance criteria — all met

| AC | Result | Evidence |
|----|--------|----------|
| Prints three delimited prompts (SM, Dev, Review) | ✅ | `=== SM ===` (line 5), `=== DEV ===` (474), `=== REVIEW ===` (999) present in stdout; 1466 lines total |
| Exit 0 on success; non-zero if a layer file missing / loader fails | ✅ | Success → exit 0. Renamed `scripts/prompts/dev/overlay.md` → exit 1 with `ERROR: Layer 3 overlay file not found...` to **stderr** |
| Does not invoke `claude` | ✅ | Dry-run handler (`scripts/ralph-loop.sh:1869-1892`) runs at top level and `exit`s before the final `main` call; `claude` is only reachable inside `run_claude()` within `main()` |
| `--help` mentions the flag | ✅ | `--dry-run-prompts  Print resolved system prompts for SM, Dev, and Review roles, then exit` under a new "Utility flags:" block |
| Checkpoint updated with `--dry-run-prompts >/dev/null` | ✅ | `DEFAULT_CHECKPOINT` in `system/ralph-loop-system.sh:128` now appends `&& $LOOP_SCRIPT --dry-run-prompts >/dev/null` |

## Checkpoint

```
bash -n ./scripts/ralph-loop.sh && bash -n ./system/ralph-loop-system.sh \
  && ./scripts/ralph-loop.sh --dry-run-prompts >/dev/null
```
→ **Checkpoint OK (exit 0)**.

## Stack-rule / guardrail compliance

- Bash only; `set -euo pipefail` preserved. The handler guards its one
  failure-prone call with `_dryrun_prompt=$(load_prompt_layers "$role") || _dryrun_rc=$?`,
  so `set -e` does not abort mid-loop. All vars initialized (`set -u` safe), no pipes (`pipefail` n/a).
- `local` correctly avoided at top level; vars namespaced `_dryrun_*`.
- No changes to `run_claude()` signature, multi-model routing, retry, salvage,
  budget caps, or upstream-fix detection. No `docs/` or `src/` files touched.
- `printf '%s\n'` used for prompt output (avoids backslash misinterpretation).

## Non-blocking observations (for the dev / story 1.4 — not blocking this pass)

1. **stdout carries 4 setup lines before `=== SM ===`.** Because the handler is
   placed at the bottom of the script, the pre-existing setup output runs first
   and lands on stdout: the `--stories all -> ...` echo (`scripts/ralph-loop.sh:263`)
   and three `log_info` persona-load lines (`log_info` writes to stdout at line 315,
   with per-run `[timestamp]` prefixes). This does not violate any of the five ACs
   (the three prompts are present and clearly delimited) and the checkpoint's
   `>/dev/null` suppresses it, so it is correctly non-blocking here.
   **However**, the story's "After This Story Completes" note says story 1.4 will
   "capture pre- and post-refactor output and verify byte-identical composition."
   The leading timestamped lines will differ run-to-run and break a naive
   `diff` of raw stdout. Story 1.4 should either compare only the
   `===`-delimited sections, or 1.3/1.4 should suppress `log_info` while
   `DRY_RUN_PROMPTS=true`. Flagging now so 1.4 plans for it. The source of the
   noise is pre-existing logging, not new code in this story.

2. **`load_prompt_layers()` was not committed by story 1.2.** Commit `5817069`
   ("feat(1.2): Add load_prompt_layers() helper") committed only the 1.2 story
   docs; the function itself lives uncommitted in the working tree and therefore
   appears inside this diff. The function code is correct and works, so no fix is
   required — but whoever commits 1.3 should ensure the 1.2 function is captured
   (ideally amended onto 1.2 or noted in 1.3's commit), so history matches the
   commit messages. Informational only; not an upstream code defect.
