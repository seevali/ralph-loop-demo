# Story 1.1: Extract Repo-Local Prompt Files

**Epic:** [Modularize Loop Prompts](../epics/modularize-loop-prompts.md)  
**Chapter:** [2026-05-24-modularize-loop-prompts](../)  
**Status:** ready-for-dev

---

## Objective

Create the `scripts/prompts/` directory structure and extract the literal text from the hardcoded prompt heredocs in `scripts/ralph-loop.sh` into separate markdown files. This is the foundation for modularizing the Ralph Loop's prompt system.

**No changes to `scripts/ralph-loop.sh` in this story** — files are extracted only. The loader function and wiring come in later stories.

---

## Context from Chapter Plan

The Ralph Loop currently hardcodes ~110 lines of agent personas and execution-context instructions as heredocs inside `scripts/ralph-loop.sh` (lines ~374–489). These are:

1. **Common block** (`common` variable, lines ~374–404) — shared by all three agents:
   - Execution Context section: non-interactive override rules ("do not greet", "skip On Activation", "do not HALT")
   - Project Conventions section: React/Vite/TypeScript stack rules
   - Scope Discipline section: story scope constraints
   - Checkpoint Command section: the build/test verification command

2. **Agent-specific wrappers** (lines ~406–490):
   - SM wrapper (lines ~406–419): conditionally includes AGENT_SM_PERSONA from `.claude/skills/bmad-create-story/SKILL.md`
   - Dev wrapper (lines ~421–434): conditionally includes AGENT_DEV_PERSONA from `.claude/skills/bmad-dev-story/SKILL.md`
   - Review wrapper (lines ~436–489): includes AGENT_REVIEW_PERSONA + Review Standards + Upstream Fix format specification

3. **Fallback stubs** (embedded in the conditional branches):
   - SM fallback (lines ~416–417): `"You are the BMAD Scrum Master..."`
   - Dev fallback (lines ~431–433): `"You are the BMAD Developer..."`
   - Review fallback (lines ~447–449): `"You are the BMAD Code Review agent."`

These files will be loaded in **Story 1.2** by a new `load_prompt_layers()` helper, assembled into the three-layer composition model, and wired into `build_system_prompts()` in **Story 1.5**.

---

## Acceptance Criteria

- ✓ `scripts/prompts/README.md` exists and explains:
  - The 3-layer composition model (Layer 1: Execution Context, Layer 2: BMAD Persona, Layer 3: Demo Rules)
  - The `{{CHECKPOINT_CMD}}` placeholder whitelist and why it matters
  - How the loader applies these layers
  - Where to make changes for a different stack

- ✓ `scripts/prompts/common/execution-context.md` exists with verbatim text from the "## Execution Context" section of the script's `common` heredoc (lines ~374–381)

- ✓ `scripts/prompts/common/project-conventions.md` exists with verbatim Layer-3 shared text:
  - The "## Project Conventions (React / Vite / TypeScript)" section (lines ~383–392)
  - The "## Scope Discipline" section (lines ~394–398)
  - The "## Checkpoint Command" section (lines ~400–404), with `{{CHECKPOINT_CMD}}` placeholder in place of `${CHECKPOINT_CMD}`

- ✓ `scripts/prompts/sm/overlay.md` exists (may be a minimal placeholder; SM-specific Layer-3 content)

- ✓ `scripts/prompts/dev/overlay.md` exists (may be a minimal placeholder; Dev-specific Layer-3 content)

- ✓ `scripts/prompts/review/overlay.md` exists with the full Review Standards + UPSTREAM_FIX_REQUIRED format block:
  - "Review Standards" section: the PASS/BLOCK criteria (lines ~456–473)
  - "Cross-Story Root Cause Analysis" section: the UPSTREAM_FIX_REQUIRED format specification (lines ~475–489)

- ✓ `scripts/prompts/bmad-fallbacks/sm.md` exists with the current inline fallback stub from lines ~416–417

- ✓ `scripts/prompts/bmad-fallbacks/dev.md` exists with the current inline fallback stub from lines ~431–433

- ✓ `scripts/prompts/bmad-fallbacks/review.md` exists with the current inline fallback stub from lines ~447–449

- ✓ No changes to `scripts/ralph-loop.sh` — the script runs unchanged after story 1.1 completes

---

## Implementation Steps

### 1. Create the directory structure

```bash
mkdir -p scripts/prompts/common
mkdir -p scripts/prompts/sm
mkdir -p scripts/prompts/dev
mkdir -p scripts/prompts/review
mkdir -p scripts/prompts/bmad-fallbacks
```

Verify the structure:
```bash
find scripts/prompts -type d | sort
```

Expected output:
```
scripts/prompts
scripts/prompts/common
scripts/prompts/dev
scripts/prompts/review
scripts/prompts/sm
scripts/prompts/bmad-fallbacks
```

### 2. Create `scripts/prompts/README.md`

This file explains the 3-layer model and serves as the reference for anyone modifying prompts.

**Content to include:**
- Overview of the three-layer composition model (what each layer represents, why the ordering matters)
- Explanation of when each layer is used and how they're loaded
- Description of the `{{CHECKPOINT_CMD}}` placeholder and why it's whitelisted (mention that per-invocation variables like story IDs MUST stay in user prompts to preserve cache hits)
- Guidance for users forking this demo for a different stack (where to edit, what each section controls)
- Note about the lifecycle: Layer 1+3 are repo-local and stable; Layer 2 is loaded fresh from `.claude/skills/` each run, or falls back to `bmad-fallbacks/` if absent

Use the chapter plan's [detailed explanation](../README.md) (§ "Recommended path" and § "Loading mechanism") as reference, but make it accessible to developers who aren't familiar with the refactor.

### 3. Create `scripts/prompts/common/execution-context.md`

Extract lines **374–381** from `scripts/ralph-loop.sh` (the "## Execution Context" section).

**Exact text to copy (preserving all whitespace):**
```markdown
## Execution Context

You are running non-interactively in an automated pipeline. Follow these rules:
- Do NOT greet the user, do NOT present menus, do NOT HALT for input.
- Skip any 'On Activation' or 'Load config' sections from your BMAD persona.
- Execute the task directly and return when done.
- If you need information not provided, make a reasonable assumption and document it in your output.
- Do not ask clarifying questions — commit to a direction and note your assumption.
```

### 4. Create `scripts/prompts/common/project-conventions.md`

Extract lines **383–404** from `scripts/ralph-loop.sh`, which includes three sections:

**Exact text to copy (note the escaped backticks in the original):**
```markdown
## Project Conventions (React / Vite / TypeScript)

- Stack: React 19 + Vite + TypeScript (`strict` on) in `src/`. Do not introduce Next.js, SSR, or any other framework.
- Function components and hooks only. No class components. Reach for `useReducer` only where it genuinely simplifies state.
- TypeScript strict mode: no `any` to silence the compiler, no `@ts-ignore`/`@ts-expect-error` to hide a real type error, no non-null `!` to paper over a possibly-undefined value. Fix the type, do not suppress it. A `tsc` error is a build break.
- Lean stack — this is a deliberate constraint. Do NOT add a dependency to `src/package.json` (UI kit, charting lib, state lib, axios/swr/react-query, etc.) unless the story spec explicitly requires it. Use what's there: native `fetch` for HTTP, React hooks for state, plain CSS or CSS Modules for styling, inline SVG for charts.
- Data fetching with native `fetch`. Handle loading and error states explicitly; do not swallow rejected promises.
- Persistence is `localStorage` only. No IndexedDB, no backend, no database. Guard `JSON.parse` of stored values against malformed data.
- Tests: Vitest + React Testing Library, colocated as `Component.test.tsx` beside `Component.tsx`. Prefer behavior-level assertions (what the user sees / what endpoint is called) over implementation detail.
- Keep imports inside `src/`. Do not reach outside the app directory.

## Scope Discipline

- Implement only what the current story spec asks for. No refactors of unrelated code, no "while I'm here" cleanups, no speculative abstractions.
- If a story seems to require something the stack rules forbid (e.g. a new library), flag it as a question in your output rather than silently installing it.
- Acceptance criteria are the contract. Make them demonstrable; do not gold-plate beyond them.

## Checkpoint Command

The project checkpoint command is: {{CHECKPOINT_CMD}}

Run this from the repo root to verify the app builds and tests pass.
```

**Critical detail:** Replace the bash variable `${CHECKPOINT_CMD}` with the literal placeholder `{{CHECKPOINT_CMD}}` (double braces). This placeholder will be substituted at load time by the loader function (added in story 1.2). This preserves the exact string for byte-identical verification in story 1.4.

### 5. Create `scripts/prompts/sm/overlay.md`

Create a minimal placeholder. The SM role does not have special review standards or additional constraints beyond the common layers, so this can be very short.

**Content:**
```markdown
<!-- SM role — no additional overlay rules beyond common project conventions -->
```

Or leave it empty (a zero-byte file is acceptable).

### 6. Create `scripts/prompts/dev/overlay.md`

Create a minimal placeholder. The Dev role does not have special review standards or additional constraints.

**Content:**
```markdown
<!-- Dev role — no additional overlay rules beyond common project conventions -->
```

Or leave it empty.

### 7. Create `scripts/prompts/review/overlay.md`

Extract lines **456–489** from `scripts/ralph-loop.sh`. This includes the Review Standards section and the Cross-Story Root Cause Analysis (UPSTREAM_FIX_REQUIRED) specification.

**Exact text to copy (note escaped backticks and quotes):**
```markdown
## Review Standards

PASS the review when the acceptance criteria are met AND the checkpoint
(`cd src && npm run build && npm test --if-present`) succeeds — even if you
would have written the code differently. Surface at most ONE blocking issue
per review pass; let the Fix step land it before reviewing again.

BLOCK only on:
1. Acceptance criteria not met — any story AC is unsatisfied or not demonstrable.
2. Build or test failure — `tsc`/Vite build errors, or failing Vitest tests. Run the checkpoint to confirm.
3. Stack-rule violation — a class component; a new `src/package.json` dependency the story did not call for (UI kit, charting/state/HTTP lib); use of localStorage-forbidden persistence; a non-`fetch` HTTP path.
4. Type-safety escape hatch hiding a real error — `any`, `@ts-ignore`/`@ts-expect-error`, or non-null `!` used to silence the compiler rather than fix the type.
5. Import reaching outside `src/`.
6. Real bug or missing error/loading handling — unhandled rejected fetch, blanked-out UI on error where the AC says otherwise, unguarded `JSON.parse` of localStorage.
7. Security issue.

DO NOT block on style: renames, comment density, test organization, or
"I'd structure this differently." Those are nits, not blockers.

## Cross-Story Root Cause Analysis

If you find an issue whose root cause is in code written by a PREVIOUS story
(not the current story being reviewed), you MUST include a structured marker
block in your review output. The format is exactly:

UPSTREAM_FIX_REQUIRED: <story-id>
ROOT_CAUSE: <one-line description of what is wrong in the upstream story's code>
AFFECTED_FILES: <comma-separated list of files in the upstream story that need fixing>
CURRENT_IMPACT: <how this upstream bug manifests in the current story>

Place this block AFTER the REVIEW_FAILED line and BEFORE detailed findings.
Include at most ONE upstream fix marker per review. Only use this marker when
the fix MUST happen in the upstream story's code — not when the current story
could reasonably work around the issue.
```

### 8. Create `scripts/prompts/bmad-fallbacks/sm.md`

Extract lines **416–417** from `scripts/ralph-loop.sh` (the SM fallback stub used when AGENT_SM_PERSONA is empty).

**Exact text to copy:**
```
You are the BMAD Scrum Master (bmad-create-story). Produce exactly one detailed, implementation-ready story spec from the epic and PRD. Acceptance criteria must be observable from outside the code.
```

Note: This is plain text, not markdown. No markdown formatting needed.

### 9. Create `scripts/prompts/bmad-fallbacks/dev.md`

Extract lines **431–433** from `scripts/ralph-loop.sh` (the Dev fallback stub).

**Exact text to copy:**
```
You are the BMAD Developer (bmad-dev-story). Implement the story spec to satisfy its acceptance criteria, then make the checkpoint pass.
```

Plain text, no markdown formatting.

### 10. Create `scripts/prompts/bmad-fallbacks/review.md`

Extract lines **447–449** from `scripts/ralph-loop.sh` (the Review fallback stub).

**Exact text to copy:**
```
You are the BMAD Code Review agent.
```

Plain text, no markdown formatting.

### 11. Verify extraction

Run the following to confirm all files exist:

```bash
ls -la scripts/prompts/README.md \
       scripts/prompts/common/execution-context.md \
       scripts/prompts/common/project-conventions.md \
       scripts/prompts/sm/overlay.md \
       scripts/prompts/dev/overlay.md \
       scripts/prompts/review/overlay.md \
       scripts/prompts/bmad-fallbacks/sm.md \
       scripts/prompts/bmad-fallbacks/dev.md \
       scripts/prompts/bmad-fallbacks/review.md
```

All nine files should exist and be readable.

### 12. Verify script is unchanged

Confirm that `scripts/ralph-loop.sh` has not been modified:

```bash
git diff scripts/ralph-loop.sh
```

Should output nothing (or show only the extraction was a new addition, not a modification).

---

## Verification Checklist

- [ ] `scripts/prompts/` directory tree created with all subdirectories
- [ ] All nine files created and readable
- [ ] `scripts/prompts/README.md` explains the 3-layer model, placeholder substitution, and fork guidance
- [ ] `execution-context.md` contains verbatim "## Execution Context" section (~8 lines)
- [ ] `project-conventions.md` contains "## Project Conventions" + "## Scope Discipline" + "## Checkpoint Command" sections (~25 lines total)
- [ ] `{{CHECKPOINT_CMD}}` (double braces) appears in project-conventions.md, not `${CHECKPOINT_CMD}`
- [ ] `sm/overlay.md` and `dev/overlay.md` exist (may be minimal/empty)
- [ ] `review/overlay.md` contains full Review Standards + Upstream Fix format specification (~35 lines)
- [ ] All three `bmad-fallbacks/` files contain the exact fallback stubs from the script
- [ ] No trailing newlines or whitespace differences in extracted files (they will be flagged in story 1.4's byte-diff)
- [ ] `scripts/ralph-loop.sh` remains exactly as it was (no modifications to the script itself)

---

## Technical Notes

### Placeholder Substitution

The `{{CHECKPOINT_CMD}}` placeholder uses double braces to distinguish it from bash variable syntax. The loader function (story 1.2) will apply a single substitution pass using a whitelisted variable set containing only `CHECKPOINT_CMD`. This preserves byte-identical output across invocations within a run (prompt cache requirement).

### Why extract these specific sections?

The split into "execution-context" and "project-conventions" reflects the conceptual three-layer model:
- **Layer 1 (execution-context.md):** Overrides that win against BMAD persona's interactive instructions
- **Layer 2:** BMAD persona (loaded from `.claude/skills/` or fallback to `bmad-fallbacks/`)
- **Layer 3 (project-conventions.md + overlays):** Demo-specific stack rules and review standards

The loader (story 1.2) will assemble these three layers with separators (`---`) to build the final cached system prompt.

### Why `.md` extension?

Markdown files are easier to edit and version-control than shell heredocs. The loader will treat them as plain text (no markdown rendering) — the `.md` extension is for tooling and human readability only.

### Fallback stubs

The `bmad-fallbacks/` directory provides hardcoded agent instructions used when BMAD persona files are missing (e.g., `.claude/skills/` not installed). These are short, generic descriptions that keep the loop functional even without BMAD.

### Next story dependency

Story 1.2 depends on all nine files created in this story. The developer of story 1.2 will implement `load_prompt_layers()` and wire it into `build_system_prompts()`. If any file is missing or has syntax errors, story 1.2 will fail.

---

## Edge Cases & Gotchas

1. **Whitespace and newlines matter** — Story 1.4 will compare byte-for-byte. Any extra trailing newlines, missing spaces, or altered indentation will be caught by the diff gate. Copy text *exactly* as it appears in the script.

2. **Escaped characters in the script** — The script uses `\"` for escaped quotes and `` \` `` for escaped backticks inside the bash string. Preserve these escapes in the extracted files (the loader will handle unescaping).

3. **The `${CHECKPOINT_CMD}` must become `{{CHECKPOINT_CMD}}`** — This is the only variable substitution allowed in Layer 3. Every other value is literal.

4. **Empty overlay files are OK** — `sm/overlay.md` and `dev/overlay.md` may be empty or contain just a comment. Story 1.2 will handle empty layers gracefully.

5. **Directory paths are repo-relative** — All paths assume `scripts/ralph-loop.sh` as the reference point. The loader will resolve them relative to `REPO_ROOT`.

---

## Cold-Start Context

**Ralph Loop:** The orchestrator script that drives the SM → Dev → Review → Fix cycle for each story in a demo project. Each step runs in a fresh Claude Code session. See the root [README.md](../../../../README.md) for the full pattern explanation.

**BMAD Method (v6.7+):** A framework of role-based agents installed via `npx bmad-method install`. Creates `.claude/skills/` with persona files for each role (SM = `bmad-create-story`, Dev = `bmad-dev-story`, Review = `bmad-code-review`).

**Prompt cache:** Anthropic's cost-saving feature that caches repeated system prompts across invocations within a 5-minute window. Requires byte-identical system prompts to hit. The loop uses `--append-system-prompt` to inject cached prompts into each agent invocation.

**Three-layer composition:** The design pattern this chapter introduces. Each agent's cached system prompt is assembled from three concatenated markdown files: Layer 1 (execution-context override), Layer 2 (BMAD persona), Layer 3 (demo stack rules). Separators (`---`) delimit the layers visibly.

**Chapter plan:** See [../README.md](../README.md) for full design details, motivation, file layout diagram, and test plan. This story's content is extracted verbatim from the chapter's findings and the current script.

---

## References

- **Chapter plan:** [../README.md](../README.md) — design rationale, file layout, loading mechanism pseudocode, migration sequence, test plan
- **Chapter PRD:** [../prd.md](../prd.md) — business-level goals and success criteria
- **Epic:** [../epics/modularize-loop-prompts.md](../epics/modularize-loop-prompts.md) — all six stories and their dependencies
- **Current script:** [../../../../scripts/ralph-loop.sh](../../../../scripts/ralph-loop.sh) — source of all extracted text (lines ~374–489)
- **Root CLAUDE.md:** [../../../../CLAUDE.md](../../../../CLAUDE.md) — stack rules and System Track agent behavior
- **Root README:** [../../../../README.md](../../../../README.md) — repo architecture, two-track explanation, Ralph Loop overview

---

## Status

**Ready for dev.** All context is provided. No blocking dependencies; all extracted text is from the current script (lines are stable). Story 1.2 depends on this story's outputs.
