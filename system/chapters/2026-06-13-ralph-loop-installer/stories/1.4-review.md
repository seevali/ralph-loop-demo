REVIEW_PASSED

# Story 1.4 — Preflight checks module — Code Review

**Reviewed:** 2026-06-13
**Verdict:** PASS — all acceptance criteria met, checkpoint green, no stack-rule or scope violations.

## What was reviewed

- `installer/src/preflight.js` — `preflight(opts)` + `renderChecklist(results, opts)`
- `installer/src/preflight.test.js` — 21 unit tests (node:test)
- `installer/package.json` — test glob change
- Story spec `1.4.md` and implementation summary `1.4-done.md`

## Verification performed

- `cd installer && npm test` → **22/22 pass, EXIT=0** (21 preflight + 1 placeholder).
- Checkpoint: `bash -n scripts/ralph-loop.sh && bash -n system/ralph-loop-system.sh && scripts/ralph-loop.sh --dry-run-prompts` → **CHECKPOINT OK** (dry-run exit 0).

## Acceptance criteria

- **AC1 (Node hard-fail):** `node.status='fail'` for major < 20; message includes `nodejs.org/download`. Module documents `node.status==='fail' → caller exit(1)`. ✓
- **AC2 (git warn):** missing git → `warn` with apt/brew/git-scm install hints. ✓
- **AC3 (jq warn + hints):** missing jq → `warn` with all four per-OS hints (`apt install jq`, `brew install jq`, `choco install jq`, `winget install jqlang.jq`), asserted in tests. ✓
- **AC4 (claude warn):** present → `pass`, absent → `warn`, never `fail`. ✓
- **AC5 (bash detection):** `win32` + no bash → `fail` with WSL2 guidance (documented exit 2); otherwise `pass`. Git-Bash-present-on-Windows correctly passes. ✓
- **AC6 (TTY checklist):** glyphs (✓/⚠/✗) + picocolors on TTY; plain key–value text off TTY; `NO_COLOR` strips ANSI while keeping glyphs. ✓
- **AC7 (unit tests):** all four required verifications present — per-check present/absent responses, TTY true/false rendering, Node hard-fail (<20), Windows-no-bash. ✓ (see note below)
- **AC8 (return structure):** `{node, git, jq, claude, bash}`, each with `status`/`message`, `node` also `version`; shape asserted by test. ✓

## Notes (non-blocking)

- **AC7 mechanism deviation:** the spec suggested PATH stubbing / `Module.prototype.require` monkey-patching; the implementation injects `commandExists`, `nodeVersion`, `platform`, and render `isTTY`/`noColor`/`writeFn` instead. This is a documented, cleaner choice that is functionally equivalent for ESM and satisfies every AC7 sub-bullet. The exit-code checks (AC7) are verified via `status` values rather than literal `process.exit()` calls — correct, since §1.3 of the spec mandates the module never call `process.exit()` and leaves exit codes to the caller. Means-to-an-end deviation; intent fully met.

## Stack & scope compliance

- Node.js confined to `installer/` (System Track rule satisfied). ✓
- No new dependency added — `picocolors` was already declared in Story 1.2; `package.json` change is only the test glob (`src/**/*.test.js`), within the Developer's allowed surface. ✓
- Imports limited to `node:child_process` and `picocolors`; nothing reaches outside `installer/`. ✓
- No edits to `docs/`, `src/`, or loop-semantic sections of `scripts/ralph-loop.sh`. ✓
- Error handling: command detection wrapped in try/catch returning boolean; no unhandled rejections. ✓

No upstream/cross-story root cause issues found.
