REVIEW_PASSED

# Story 2.1 Code Review — Target-directory classifier

**Reviewed:** 2026-06-13
**Verdict:** PASS — acceptance criteria met, checkpoint green, stack rules honored.

## What was reviewed

- `installer/src/classify.js` — the `classifyTarget(dirPath)` classifier module.
- `installer/test/classify.test.js` — 9 unit tests.
- Story spec `2.1.md` (acceptance criteria) and implementation summary `2.1-done.md`.

## Verification performed

| Check | Result |
|-------|--------|
| `node -c installer/src/classify.js` | syntax OK |
| `cd installer && npm test` (`node --test`) | **31 passed, 0 failed** (21 preflight + 9 classify + 1 placeholder) |
| Project checkpoint: `bash -n scripts/ralph-loop.sh && bash -n system/ralph-loop-system.sh && scripts/ralph-loop.sh --dry-run-prompts` | exit 0 |
| No new npm dependencies | `installer/package.json` unchanged; module uses only `node:fs`, `node:path` |

## Acceptance criteria assessment

- **AC1 (empty):** Empty directory → `empty`. ✓ (see contradiction note below re: `.git/`)
- **AC2 (existing-project):** All listed signals present in `FILE_SIGNALS` (`.git`, `package.json`, `Gemfile`, `pyproject.toml`, `pom.xml`, `go.mod`, `Cargo.toml`, `composer.json`, `pubspec.yaml`) plus `*.sln` via `endsWith('.sln')`. Returns `existing-project` with manifest absent. ✓
- **AC3 (existing-install):** `.ralph/manifest.json` present → `existing-install` regardless of other signals (manifest checked before signal-count branch). ✓
- **AC4 (ambiguous):** Manifest present + missing installer-owned files → `existing-install`. Test 5 confirms. The AC's "advisory note" is explicitly scoped as "used in Story 2.3 conflict detection, not blocking here"; the returned `hasManifest`/`signals` give Story 2.3 what it needs. Deferral is consistent with the AC's own wording. ✓
- **AC5 (tests):** 9 tests covering empty, existing-project (Node + Python), existing-install, ambiguous, multiple-signals, non-existent path, symlink, and `.sln`. Fixtures created at test time via `fs.mkdtemp`/`writeFile`, cleaned up in `finally`. ✓

## Error handling (sound)

- `fs.realpath` resolves symlinks and existence in one call; `ENOENT` → "not found", `EACCES` → permission guidance.
- `fs.stat` after realpath rejects non-directory targets with a clear message.
- `readdir` `EACCES` re-thrown with a `chmod +rx` hint.
- `.ralph` pushed once (only when `hasManifest`), avoiding double-counting since it is not in `FILE_SIGNALS`.

## Non-blocking advisories (no fix required)

1. **Spec-internal contradiction, resolved correctly.** AC1's parenthetical "(or only `.`, `..`, `.git/`) classifies as `empty`" contradicts AC2, which lists `.git` as a project signal. The implementation follows AC2 (a `.git`-only directory → `existing-project`), which matches the dominant design intent throughout the spec (Technical Details, Case 2, Case 5). This is a spec defect, not an implementation defect — flagging for the SM to reconcile the AC1 wording in a future edit, not for the Dev to change behavior.
2. **No explicit advisory field for the partial-install case.** Per AC4's "not blocking here," this is intentionally left for Story 2.3 to derive from `hasManifest` + `signals`. Noted so Story 2.3 knows the field does not yet exist.

No upstream (cross-story) root cause found; no `UPSTREAM_FIX_REQUIRED` marker needed.
