REVIEW_PASSED

# Story 2.2 Code Review — Wizard flow building the InstallPlan

**Reviewed:** 2026-06-13
**Verdict:** PASS — all acceptance criteria met; full test suite and System Track checkpoint green. The AC4 git-init gap flagged in the prior review pass is now resolved.

## Scope reviewed

- `installer/src/wizard.js` (new, 294 lines) — interactive wizard collecting the in-memory `InstallPlan` plus exported `validators`
- `installer/src/wizard.test.js` (new) — 34 unit tests
- `installer/bin/ralph.js` (modified) — `install` command wiring preflight → classify → wizard
- Supporting context: `installer/src/classify.js`, `installer/package.json`, story spec `2.2.md`, implementation summary `2.2-done.md`

## Verification performed

- **Full installer test suite:** `npm test` → `tests 65 / pass 65 / fail 0` (34 wizard + 20 preflight + 10 classify + 1 placeholder). Run directly.
- **System Track checkpoint:** `bash -n scripts/ralph-loop.sh && bash -n system/ralph-loop-system.sh && scripts/ralph-loop.sh --dry-run-prompts` → exit 0.

## Acceptance criteria assessment

- **AC1 — Enter-only complete InstallPlan:** All required fields present with documented defaults (`appDir: "src"`, `checkpointCommand: "npm run build && npm test"`, `loopRetries: 3`, `maxTokensPerTurn: 200000`, `modelOrder: ['opus','sonnet','haiku']`). The wizard performs no filesystem operations — `wizard.js` imports no `fs`/`child_process`; it only calls `console.log`, `intro`, and `outro`. Confirmed by happy-path and schema-completeness tests. ✓
- **AC2 — Ctrl-C / decline leaves zero effects, exit 0:** `checkCancel()` detects the clack cancel symbol, prints "Nothing was changed.", calls `exit(0)`, and throws the internal `CANCELLED` sentinel; the outer `try/catch` returns `undefined`. `bin/ralph.js:59-62` guards the `undefined` return and exits 0. Decline at the final confirm routes through `decline()` identically. Cancel-at-explainer, cancel-mid-wizard, and decline-at-final tests all pass. Declining git-init correctly continues the flow (a valid answer, not a cancellation). Live-TTY manual verification is correctly deferred to Story 4.1 per spec. ✓
- **AC3 — Output validates at collection time:** Each `text()` prompt has a named `validate` callback (exported as `validators.*` for isolated testing). Integer fields enforce `>= 0` (`loopRetries`) and `> 0` (`maxTokensPerTurn`); string fields reject empty/whitespace and are trimmed into the returned plan; `isUpdate` derived correctly from `existing-install`. ✓
  - *Spec-sanctioned deferral:* AC3's "skip prompts locked to the existing install" for `existing-install` is not implemented, but Edge Case 4 of the same spec explicitly instructs treating the update case as a fresh install for this story and deferring nuanced keep/update semantics to Story 3.2. Not a blocker.
- **AC4 — Integrates with preflight and classification (prior block now RESOLVED):** `bin/ralph.js:30-54` chains `preflight() → renderChecklist() → classifyTarget() → runWizard(targetDir, classifyResult.type, preflightResults)`. The empty-directory git-init offer is now a real `confirm()` prompt (`wizard.js:121-130`, "Initialize a git repository as part of the install?", `initialValue: true`, routed through `checkCancel`) whose answer is stored as `runGitInit` on the `InstallPlan`. Actual `git init` execution is deferred to Story 2.3, preserving AC2's zero-side-effects property. The prompt is offered only for `empty`; tests assert `runGitInit` defaults to `true` for empty, can be declined to `false`, and is `false`/not-offered for `existing-project` and `existing-install`. ✓
- **AC5 — Edge cases:** Whitespace-padded inputs trimmed in the returned plan; markdown/special characters and embedded `\n` stored verbatim (escaping deferred to Story 2.3 per Edge Case 2); non-TTY without injected prompts throws a helpful error. All covered by passing tests. The prior non-blocking note about an unguarded `runWizard` call is also addressed — `bin/ralph.js:53-58` now wraps it in a try/catch that prints a clean message and exits 1. ✓

## Stack & guardrail compliance

- Node.js confined to `installer/` (System Track Story 1.1 exception). ✓
- No new dependencies beyond those scaffolded in Story 1.2 (`@clack/prompts`, `commander`, `picocolors`). ✓
- No new top-level directories; no imports reaching outside `installer/`. ✓
- No filesystem/network side effects in the wizard; checkpoint command unmodified. ✓
- Test colocated as `src/wizard.test.js` per convention. ✓

## Notes (non-blocking)

- Dependency injection via `opts.prompts` / `opts.exit` mirrors the `preflight.js` pattern and keeps the flow testable without a real TTY — sound choice.
- Using `isCancel()` return-value checks instead of the spec's misleading try/catch example is correct for `@clack/prompts` and is documented in the summary.
- `runGitInit` is an addition beyond the spec's literal InstallPlan shape; it is consistent with AC4's git-init requirement and is consumed by Story 2.3. Acceptable.
- The summary line `Stack: ${...split('\n')[0]}...` always appends `...` even for single-line descriptions. Cosmetic only.

No blocking issues found. No upstream (cross-story) root cause identified.
