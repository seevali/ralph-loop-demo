REVIEW_PASSED

# Story 2.4 Code Review — BMAD install step

**Reviewed:** 2026-06-13
**Verdict:** PASS — all acceptance criteria met, checkpoint green.

## What was reviewed

- `installer/src/bmad.js` — new BMAD install orchestration module
- `installer/src/bmad.test.js` — 5 unit tests
- `installer/src/writer.js` — integration of the BMAD step into `writeInstall()`
- `installer/src/wizard.js` — added BMAD confirm prompt + `skipBmad` field
- `installer/src/wizard.test.js` — `skipBmad` tests + shifted confirm indices
- `installer/src/writer.test.js` — 3 new writer↔BMAD integration tests

## Acceptance criteria

| AC | Status | Evidence |
|----|--------|----------|
| AC-1 Module exports & integrates | ✓ | `bmad.js:47` exports `installBmad()`; `writer.js:5,551,577` imports and invokes it conditionally. (Story said "default function"; a named export is used and integrates cleanly — functionally equivalent, not a blocker.) |
| AC-2 Exact argv unit test | ✓ | `bmad.test.js:7-35` asserts `execFile` called with `npx` + the exact ordered `BMAD_ARGS` array (`bmad.js:7-15`). |
| AC-3 Failure path non-fatal | ✓ | `bmad.js:69-73` catches, stops spinner, prints manual command, returns `{ success: true }`; verified by `bmad.test.js:57-97` (no throw, manual command + guidance printed). |
| AC-4 Non-TTY handling | ✓ | `bmad.js:48,58-61` uses a noop spinner + plain `log('Installing BMAD modules...')`; `bmad.test.js:101-120` asserts no ANSI escapes and a plain progress line. |
| AC-5 Writer integration | ✓ | `writer.js:576` gates the call on `plan.skipBmad === false`; `writer.test.js:801-887` covers false→called, true→skipped, undefined→skipped (backward compat). |
| AC-6 Checkpoint | ✓ | See below. |

## Checkpoint results

```
cd installer && npm test           → 112/112 pass, 0 fail
bash -n scripts/ralph-loop.sh      → exit 0
bash -n system/ralph-loop-system.sh→ exit 0
./scripts/ralph-loop.sh --dry-run-prompts → OK
```

## Stack / guardrail compliance

- Node.js confined to `installer/` (permitted per Story 1.1). No language switches elsewhere.
- No new `package.json` dependency: uses built-in `node:child_process` / `node:util` and dynamically imports the already-present `@clack/prompts` only in the TTY branch.
- `execFile` invoked with a fixed argv array and no shell — no command-injection surface; no user input is interpolated into the command.
- BMAD stdout is suppressed by default (`execFile` buffers, does not inherit); stderr is captured for the failure message (`bmad.js:71`).
- Dependency-injection pattern (`opts.execFile`, `opts.log`, `opts.installBmad`) matches the existing codebase convention, keeping tests deterministic without spawning real processes.
- No changes to `docs/`, `src/`, or the loop-semantic sections of `ralph-loop.sh`.

## Non-blocking observations (not defects)

- The TTY branch (`bmad.js:55-57`, dynamic `@clack/prompts` spinner) is never exercised by the test suite — tests always supply a spinner or set `isTTY:false`. The spinner contract (`start`/`stop`) is compatible, so this is a coverage note only.
- Windows `npx.cmd` would need `shell:true` for `execFile`, but the story (Edge Case 7) explicitly accepts the no-special-handling stance and the primary environment is Linux/WSL. Not a defect.
- `bmad.test.js:112` asserts `!output.includes('[')` which is broader than just ANSI detection; it passes given the current plain-text message and is harmless.

No blocking issues found.
