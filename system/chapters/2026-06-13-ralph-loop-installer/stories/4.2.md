# Story 4.2: Documentation rewrite

**Chapter:** [Ralph Loop Guided Installer](../README.md)  
**Epic:** [Epic 4: Polish and release](../epics/ralph-loop-installer.md#epic-4-polish-and-release)  
**Date:** 2026-06-13  
**Status:** review

---

## Summary

Update the repo's README and TIMELINE to reflect the installer-first workflow. The README now leads with a quick-start install command (linking to deeper docs), preserves manual install steps in a fallback section, and updates the "Adapting this to your project" section to prioritize the installer. Add a `[System]` entry to TIMELINE.md logging this chapter's completion.

---

## Acceptance Criteria

1. **README quick start contains a single install command** (the `npx` invocation) with prerequisites above it and a link to deeper docs below.
2. **Old manual steps preserved under "Manual install" fallback section** — still accurate but de-emphasized.
3. **"Adapting this to your project" section leads with the installer** — moving from manual instructions to "run the installer" as the primary path.
4. **TIMELINE.md entry added** for the chapter, tagged `[System]`, summarizing the chapter outcome with a commit link.

---

## Dependencies

**Previous stories (must be complete):**
- Story 1.1: Stack rules amended; `CLAUDE.md` and `README.md` updated with `installer/` directory.
- Story 1.2 through 2.5: Installer package scaffolded, wizard built, BMAD integration, doctor command, outro.
- Story 3.1 through 3.3: Non-interactive mode, update flow, uninstall logic.
- Story 4.1: E2E suite complete and passing.

The installer command is now functional: `npx <package> install` works in both interactive and non-interactive (`--yes`) modes.

**Note on package name:** Story 4.3 (Release preparation) finalizes the npm package name. This story uses the placeholder `<package>` in examples; on implementation, substitute the resolved name (likely `@seevali/ralph-loop` or `ralph-loop-installer` per PRD §Prior art).

---

## Technical Details

### File 1: Root `README.md`

**Target:** `/home/seevali/projects/Metis/demos/ralph-loop-demo/README.md`

**Current structure** (inferred from typical ralph-loop repo README):
- Intro explaining the Ralph Loop concept
- "Getting Started" or "Installation" section with manual steps
- "Adapting this to your project" with manual customization steps
- Other sections (architecture, repo layout, etc.)

**Modifications:**

#### 1.1 Quick Start Section (new, immediately after the intro)

Insert a new `## Quick Start` section right after the intro (before any other sections). Content:

```markdown
## Quick Start

### Prerequisites

- **Node.js 20+** (the installer runs via `npx`)
- **`claude` CLI** ([install](https://github.com/anthropics/claude-code)) — the installer warns if missing, but your loop won't run without it.
- **Git** (optional but recommended — the installer can initialize a repo)
- **`jq`** (optional — the installer provides OS-specific install commands if it's missing)

### Install in 5 minutes

```bash
npx <package> install
```

That's it. The installer asks about your project, scaffolds the loop, and walks you through customization. For non-interactive use (CI/scripts):

```bash
npx <package> install --yes
```

**Next:** The installer generates `GETTING-STARTED.md` with your project-specific next steps. Start reading there.

### Why this approach?

The Ralph Loop is powerful but requires careful setup: choosing a stack, writing the initial PRD and epic stubs, customizing system prompts, installing BMAD. The installer automates this, enforces best practices, and is designed to run on every update without losing your work. See the [repo layout](#repo-layout) and [architecture](#architecture) sections below for what gets installed.
```

**Rationale:** Mirrors the pattern from `bmad-method install` (referenced in PRD §Background); leads with one command + prerequisites + a link to deeper content.

#### 1.2 Reposition or rename "Getting Started" / "Installation"

The existing installation section (currently manual steps like "clone the repo, copy files, write PRD stubs") should be moved and renamed:

- **Find** the current manual installation section (likely `## Getting Started`, `## Installation`, or similar).
- **Rename** to `## Manual Install (Fallback)` or similar.
- **Move** it to near the end of the README (after core sections like architecture, repo layout, but before any appendices or metadata).
- **Preserve all existing steps verbatim** — this is for users who need or prefer manual control.
- **Add a note at the top of this section:**

```markdown
## Manual Install (Fallback)

The recommended approach is the [Quick Start](#quick-start) above. This section documents the manual installation process for those who need per-step control or are adapting the loop to a non-standard environment.
```

#### 1.3 Update "Adapting this to your project" section

**Find:** The current section (by name or paraphrase) that explains how to customize the loop for a new project.

**Replace the opening with:**

```markdown
## Adapting this to your project

The easiest path is to run `npx <package> install` in your target directory. The installer asks about your project, renders customized templates, and updates your system prompts. Re-run to update without touching your work.

If you're adapting the loop for a framework or environment not covered by the installer, or you need fine-grained control:

1. [Follow the Quick Start](#quick-start) to get a baseline install.
2. Edit the installer-generated files (described below) for your stack.
3. Test with `./scripts/ralph-loop.sh --dry-run-prompts >/dev/null`.

### Files you'll customize
```

Keep the existing subsection-level details (specific files to edit, conventions to follow) but reframe them as "*after* the installer" rather than "*instead of* the installer."

#### 1.4 Update any references to manual copy/setup

Scan the entire README for instructions like:
- "Copy `scripts/ralph-loop.sh` into your project"
- "Write your PRD in `docs/prd.md`"
- "Edit `system/CLAUDE.md`"

Reframe each as "The installer handles this; if you need to customize further, edit `<file>`."

**Example:**
- **Before:** "Customize `scripts/prompts/common/project-conventions.md` for your stack — list frameworks, languages, testing tools."
- **After:** "The installer scaffolds `scripts/prompts/common/project-conventions.md` based on your answers. Customize it further if needed for frameworks the installer didn't cover."

---

### File 2: TIMELINE.md

**Target:** `/home/seevali/projects/Metis/demos/ralph-loop-demo/TIMELINE.md`

**Current structure** (assumed from the CLAUDE.md reference):
- Reverse-chronological log of repo changes
- Entries tagged `[Demo]` or `[System]`
- Format: headline + paragraph(s) + commit link(s)

**Modification:**

Add a new entry at the **top** (most recent) of the timeline. Use this template:

```markdown
## [System] Ralph Loop Guided Installer chapter complete (2026-06-13)

The installer is now complete and published. A new developer can run `npx <package> install` in an empty directory or existing project and have a working, customized Ralph Loop in under 5 minutes. The wizard teaches the concepts interactively; a `--yes` flag enables fully non-interactive mode for CI.

**What was built:** A Node.js CLI (in `installer/`, permitted by story 1.1) that preflight-checks the environment, classifies the target directory, guides the user through project configuration, scaffolds the loop, manages BMAD installation, and includes update/uninstall/doctor lifecycle commands. The e2e suite (story 4.1) validates all major workflows. Documentation (this story) emphasizes the installer as the primary path, with manual steps as a fallback.

**Why it matters:** The Ralph Loop's manual setup was a significant barrier to adoption. This installer brings the setup experience in line with `bmad-method install` and modern CLI tools like `npm create astro@latest`. Estimated time-to-running-loop: now ≤ 5 min vs. the previous ~1 hour of careful README-following.

**Repo structure:** The `installer/` directory is the sole exception to the "everything is Bash + Markdown" rule outside that folder. The installed footprint mirrors this repo's layout (scripts/, docs/, GETTING-STARTED.md, .CLAUDE.md, .gitignore entries, .ralph/manifest.json), so all existing loop documentation applies to installed projects.

**Next:** Story 4.3 finalizes the package name and release checklist; the human publishes to npm (out of scope for the loop).

[Chapter README](system/chapters/2026-06-13-ralph-loop-installer/README.md) | [Epics](system/chapters/2026-06-13-ralph-loop-installer/epics/ralph-loop-installer.md)
```

**Commit reference:** This entry will be committed in the same commit as the README changes (or a followup commit in the same story PR).

---

## Implementation Steps

1. **Open the repo's root README.md** and locate the current "Getting Started" or "Installation" section (or equivalent).

2. **Insert the new Quick Start section** (from 1.1 above) immediately after the intro/title section.

3. **Move the current manual installation section** to near the end of the README and rename it to "Manual Install (Fallback)"; add the disclaimer note.

4. **Locate the "Adapting this to your project" section** and rewrite the opening to prioritize the installer (from 1.3 above), keeping subsection details intact.

5. **Scan the entire README** for manual-setup instructions (copy/paste, write PRD stubs, edit system prompts) and reframe them as post-installer customization. Test the clarity by reading as a new user: does it feel like "run the installer, then customize" or "manually do everything"?

6. **Add the new TIMELINE.md entry** at the top of the file (most recent first).

7. **Verify the changes:**
   - The README now leads with a single `npx <package> install` command.
   - The old manual steps are fully preserved in the fallback section (copy/paste what was there; don't delete).
   - Links in the README are still valid (especially the link from "Quick Start" back to subsections like "Repo Layout").
   - TIMELINE.md entry is formatted consistently with existing entries (reverse chronological, headline + summary, links).

---

## Verification Steps

Run these checks to confirm the story is complete:

### 1. README structure check

```bash
cd /home/seevali/projects/Metis/demos/ralph-loop-demo

# Verify the Quick Start section exists and contains the install command
grep -A 5 "## Quick Start" README.md | grep -q "npx.*install" && echo "✓ Quick Start section found"

# Verify "Manual Install (Fallback)" section exists
grep -q "## Manual Install" README.md && echo "✓ Manual Install fallback section found"

# Verify the old manual steps are preserved (rough check: look for 'PRD' or 'epic' mentions)
grep -q "PRD\|epic" README.md && echo "✓ Old setup instructions preserved"
```

Expected output: all three checks pass.

### 2. README readability check

**Manual inspection:** Read the README from top to bottom as a new user. Confirm:
- [ ] The Quick Start section is easy to find (near the top, after intro).
- [ ] The install command is the first actionable step.
- [ ] Links from the Quick Start to "deeper docs" (e.g., "Repo Layout", "Architecture") resolve within the README.
- [ ] The "Adapting this to your project" section now reads as "run the installer, then customize these files" rather than "manually do all of this."
- [ ] The "Manual Install (Fallback)" section is clearly labeled as a fallback and is toward the end.

### 3. TIMELINE.md check

```bash
# Verify the new entry is at the top and is tagged [System]
head -20 TIMELINE.md | grep -q "\[System\] Ralph Loop Guided Installer" && echo "✓ TIMELINE entry found"

# Verify the entry links to the chapter
grep "system/chapters/2026-06-13-ralph-loop-installer" TIMELINE.md && echo "✓ Chapter link present"
```

Expected output: both checks pass.

### 4. Link validation

```bash
# Check for any broken markdown links in the README
# (Links should point to sections within the README or valid files in the repo)
grep -o '\[.*\](.*\.md\|#[a-z-]*' README.md | head -20
```

Manually verify a sample of links resolve to actual sections (search for the section header or file).

---

## Edge Cases & Gotchas

1. **Package name placeholder:** This story uses `<package>` as a placeholder for the npm package name (e.g., `@seevali/ralph-loop` or `ralph-loop-installer`). Story 4.3 finalizes the name. **Action for the dev:** Implement with the placeholder; Story 4.3 will update it to the final name. Do NOT hardcode a specific name without confirmation from Story 4.3's outcome.

2. **Link consistency:** The README may have section links like `[Quick Start](#quick-start)`. Ensure the markdown heading you insert is exactly `## Quick Start` (lowercase, with hyphens for spaces). Test by rendering locally if possible.

3. **Preserved manual steps must be exact:** The "Manual Install (Fallback)" section should be a verbatim cut-and-paste of the current manual steps. Do not paraphrase or clean them up — the goal is to preserve the old instructions exactly.

4. **TIMELINE format:** Check existing TIMELINE entries for the exact format (commit link style, date format, tagging convention). Maintain consistency.

5. **Subsections under "Adapting this to your project":** The existing subsections under this heading (e.g., "Customizing system prompts", "Choosing a stack") should remain intact. Only rewrite the opening paragraph(s) to prioritize the installer.

6. **Cross-references to the installer directory:** The README may not yet mention `installer/` or `npx` if this is the first time the installer is being documented. Ensure the new Quick Start section is self-contained (explains what the command does, links to prerequisites) without assuming prior knowledge.

---

## Success Criteria (Verification Checklist)

- [ ] `README.md` contains a `## Quick Start` section with the `npx <package> install` command within the first 100 lines of the file.
- [ ] The Quick Start section lists prerequisites (Node.js, claude CLI, optional: git, jq) above the command.
- [ ] The Quick Start section links to deeper documentation (e.g., "see [Repo Layout](#repo-layout)").
- [ ] The original manual installation steps are preserved **verbatim** under a "Manual Install (Fallback)" section.
- [ ] The "Adapting this to your project" section begins by recommending the installer, then details file customization.
- [ ] `TIMELINE.md` has a new entry at the top, tagged `[System]`, referencing this chapter.
- [ ] All markdown links in the README are valid (section headers match, file paths exist).
- [ ] Running `bash -n README.md` (if README is processed as shell) or a markdown linter passes (no syntax errors).

---

## Testing the Change

After implementation, do a cold-start reading:

1. **Imagine you're a new user** who found this repo and wants to install it.
2. Open `README.md` and look for the first actionable step. **Expected:** It's the quick-start install command, appearing within the first ~200 lines, clearly marked with prerequisites and a next-step link.
3. Scan for the phrase "manual" or "fallback." **Expected:** You find a late section explicitly labeled as the fallback, not the primary path.
4. Look for "Adapting." **Expected:** The section leads with "run the installer," not "manually customize files."
5. Open `TIMELINE.md` and check the most recent entry. **Expected:** It's tagged `[System]`, references the Ralph Loop Guided Installer, and links to the chapter.

If all five checks feel natural when reading as a new user, the story is ready for review.

---

## Notes for the Code Reviewer

- **Validate structure, not style:** The old manual steps must be preserved exactly (even if they could be reworded). The goal is de-emphasis via repositioning, not rewriting.
- **Check link targets:** All new or updated markdown links must point to actual sections (headers) or files.
- **Verify the placeholder:** The word `<package>` should appear as a placeholder where the final package name will go in Story 4.3. This is correct and expected.
- **TIMELINE format:** Check the new entry against 2–3 existing entries to ensure consistent formatting (date anchors, commit links, nested structure).

