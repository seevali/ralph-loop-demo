REVIEW_PASSED

# Story 3.3 — Uninstall — Code Review

**Reviewed:** 2026-06-13
**Verdict:** PASS — all seven acceptance criteria met, checkpoint clean, tests green and now stable. The prior blocking flakiness has been fixed.

## Scope reviewed

- `installer/src/uninstall.js` (new) — core uninstall module
- `installer/src/uninstall.test.js` (new) — 27 unit tests
- `installer/bin/ralph.js` (modified) — `uninstall` subcommand wiring (lines 292–313)
- Cross-checked against `installer/src/writer.js` (manifest format / ownership classes) and `installer/src/colors.js` (TTY-aware output) for consistency.

## Resolution of the prior blocking finding

The earlier review BLOCKED on a flaky checkpoint: several `uninstall()` calls exercised the success path without injecting `log`/`errLog`, so real `console.log` output interleaved with `node --test`'s V8-serialized IPC stream and intermittently produced "Unable to deserialize cloned data" (≈1 in 4–6 runs).

This is now fixed. Every previously-offending call injects silent logging:
`uninstall.test.js:260, 348, 380, 409, 434, 459, 490, 591` all pass `{ ..., log: () => {}, errLog: () => {} }`. The production module still defaults to `console` (correct behavior); only the tests stopped leaking to stdout, and no tests were dropped.

**Verification:** ran the full installer suite 17 times consecutively (`npm --prefix installer test`) — **all exit 0, 219 pass / 0 fail, zero deserialize errors.** The flakiness is gone.

## Checkpoint / verification results

- System Track checkpoint — **exit 0**:
  `bash -n ./scripts/ralph-loop.sh && bash -n ./system/ralph-loop-system.sh && ./scripts/ralph-loop.sh --dry-run-prompts >/dev/null`
- `installer` unit tests — **219 pass / 0 fail**, stable across 17 consecutive runs.

## Acceptance criteria — all satisfied

1. **Installer-owned files removed, manifest last** — Phase 3 deletes installer-owned files (skipping `.gitignore`), Phase 3b cleans the `.gitignore` Ralph Loop section, Phase 5 removes `.ralph/manifest.json` last (`uninstall.js:194-205`, `264-284`). ✓
2. **User-owned files preserved and listed; never touched without consent** — interactive path lists each relative path and prompts; decline/cancel preserves and logs `[Skipped]` (`uninstall.js:226-260`). ✓
3. **`--yes`** — skips prompt, preserves user-owned byte-identically (test asserts byte-identical content), removes installer-owned, returns success (`uninstall.js:223-225`). ✓
4. **`--force`** — removes both classes without prompting, returns success (`uninstall.js:211-222`). ✓
5. **Empty `.ralph/` removed** — `removeEmptyDir` after manifest removal (`uninstall.js:286-294`). ✓
6. **No manifest → exit 1 with helpful message** — exact string `"No Ralph Loop install found here. (Expected .ralph/manifest.json)"`; CLI maps `success:false` to `process.exit(1)` (`uninstall.js:180-185`, `ralph.js:306-308`). ✓
7. **Plain output in non-TTY / NO_COLOR** — uses `isColorEnabled` from `colors.js`; tests confirm no ANSI escapes under `NO_COLOR=1` and non-TTY (`uninstall.js:163-169`). ✓

## Consistency / standards checks

- **Manifest contract:** uninstall reads `manifest.files` as an object of `{ ownership, checksum, path }` and trusts the recorded `ownership` class rather than re-deriving it — exactly matching `writer.js:writeManifest`/`getOwnership`. No cross-story drift; no `UPSTREAM_FIX_REQUIRED` needed.
- **Stack rules (System Track):** Node.js confined to `installer/`; ESM + `node:` builtins; no new dependencies (reuses `@clack/prompts`, already used by `writer.js`); no new top-level directory. ✓
- **Imports:** all stay within `installer/`. ✓
- **Critical vs non-critical errors:** manifest-removal failure returns `success:false` regardless of `--force`; non-critical file failures are suppressed only under `--force` — matches the spec's error-handling rule and AC4/AC7. ✓
- **`.gitignore` cleanup:** removes only the `# Ralph Loop` section (bounded by blank line / next comment header), deletes the file if it becomes empty, preserves pre-existing entries — verified by both `cleanGitignore` unit tests and the end-to-end uninstall tests. ✓

## Non-blocking observations (informational only — not defects)

- Empty parent directories left by removed files (e.g. `scripts/prompts/`, `docs/`) are not pruned; only `.ralph/` is removed. No AC requires pruning them. Acceptable.
- BMAD-installed trees (`_bmad/`, `.claude/skills/`) are not in the manifest, so uninstall removes their `.gitignore` entries but not the files themselves. Out of scope for this story's ACs (manifest-driven removal); worth a future story if full teardown is desired.
- On a non-critical installer-owned removal failure without `--force`, the code collects the error and continues to remove the manifest, then exits 1, rather than prompting continue/abort (a detail in the implementation steps, not an AC). The resulting behavior is safe and deterministic. Not a blocker.

None of the above blocks per the review standards (ACs met, build/tests pass and are stable, no stack-rule violation, no import escape, no security issue within the local-tool threat model).
